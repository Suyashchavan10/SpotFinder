---
# tasks file for common
- name: Install NTP package
  apt:
    name: ntp
    state: present
  become: yes

- name: Ensure NTP service is running and enabled
  service:
    name: ntp
    state: started
    enabled: yes
  become: yes
